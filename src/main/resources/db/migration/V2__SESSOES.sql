CREATE TABLE IF NOT EXISTS SESSAO (  ID SERIAL PRIMARY KEY,  ID_PAUTA INTEGER REFERENCES PAUTA(ID),  DATA_HORA_ABERTURA TIMESTAMP NOT NULL,  DATA_HORA_ENCERRAMENTO TIMESTAMP NOT NULL,  SITUACAO INTEGER NOT NULL);